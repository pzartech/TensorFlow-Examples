# Postgres / Supabase connection string
DATABASE_URL=postgres://user:password@host:5432/ici

# Didit credentials (keep server-side only — never expose to the browser)
DIDIT_API_KEY=
DIDIT_WEBHOOK_SECRET=
